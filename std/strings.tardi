
uses: std/math
uses: std/vectors
uses: std/_strings

exports:
    <string> >string utf>string concat nth >utf8 empty? in? starts-with?
    ends-with? index-of? length replace-all split split-all split-at
    split-whitespace lines strip-start strip-end substring >lowercase
    >uppercase utf8>digit parse-int utf8>string chunk-on repeat-string
    pad0 ;

/// utf8 -- int
: utf8>digit   ( S int -- S int )
    48 -
    0 9 check-bounds ;

/// accum n -- accum'
: accumulate-place   ( S int int -- S int )  swap 10 * + ;

/// s -- i
: parse-int   ( S str -- S int )
    0
    swap >utf8
    [
        utf8>digit
        accumulate-place
    ] each ;

/// str|#f -- ?
: at-end?   ( S a -- S bool )
    [
        dup #f ==
        dup [ nip ] when
    ] [ empty? ] || ;

/// str n -- vector/str
: chunk-on   ( S str int -- S vec )
    { } -rot
    [ over at-end? ! ] [
        [
            split-at
            [ over push! ] dip
        ] keep
    ] while
    2drop ;

: repeat-test    ( S int -- S int bool )  dup 0 > ;
: repeat-iter    ( S int -- S int )       1 - ;
: repeat-extend  ( S str str -- S str str str )  dupd concat ;

/// times string -- string
/// repeats a string times times
: repeat-string   ( S int str -- S str )
    "" rot
    [ repeat-test ] [
      [ repeat-extend ] dip
      repeat-iter
    ] while
    drop nip ;

/// string width -- string
/// this pads a string with '0' to be width
: pad0   ( S str int -- S str )
    over length -
    dup 0 > [
        "0" repeat-string
        swap concat
    ] [
        drop
    ] if ;
